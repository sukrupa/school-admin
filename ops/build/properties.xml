<project name="properties">

    <property name="src.dir" value="src"/>
    <property name="src.main.dir" value="${src.dir}/main/java"/>
    <property name="src.test.dir" value="${src.dir}/test"/>
    <property name="src.test.shared.dir" value="${src.test.dir}/shared/java"/>
    <property name="src.test.unit.dir" value="${src.test.dir}/unit/java"/>
    <property name="src.test.integration.dir" value="${src.test.dir}/integration/java"/>
    <property name="src.test.functional.dir" value="${src.test.dir}/twist/functional-test"/>
    <property name="src.test.functional.src.dir" value="${src.test.functional.dir}/src"/>

    <property name="resources.main.dir" value="${src.dir}/main/resources"/>

    <property name="web.dir" value="web"/>

    <property name="lib.main.dir" value="lib/main"/>
    <property name="lib.test.dir" value="lib/test"/>
    <property name="lib.twist.dir" value="lib/twist"/>

    <property name="target.dir" value="target"/>
    <property name="target.main.dir" value="${web.dir}/WEB-INF/classes"/>
    <property name="target.test.dir" value="${target.dir}"/>
    <property name="target.test.cobertura_instrumented.dir" value="${target.test.dir}/cobertura_instrumented"/>
    <property name="target.test.shared.dir" value="${target.test.dir}/shared"/>
    <property name="target.test.unit.dir" value="${target.test.dir}/unit"/>
    <property name="target.test.integration.dir" value="${target.test.dir}/integration"/>
    <property name="target.test.twist.functional-test.dir" value="${target.test.dir}/twist/functional-test"/>
    <property name="target.test.functional.dir" value="${target.test.twist.functional-test.dir}"/>
    <property name="target.test.twist.sukrupa-code.dir" value="${target.test.dir}/twist/sukrupa-code"/>
    <property name="target.reports.dir" value="${target.dir}/reports"/>
    <property name="target.reports.cobertura.dir" value="${target.reports.dir}/cobertura/"/>
    <property name="target.reports.twist.dir" value="${target.reports.dir}/html/"/>

    <property name="war.name" value="${ant.project.name}.war"/>
    <property name="app.zip.name" value="school-admin.zip"/>

    <property name="deploy.lib.dir" value="${deploy.dir}/lib"/>
    <property name="deploy.staging.server" value="10.7.4.74"/>
    <property name="deploy.staging.user" value="go"/>
    <property name="deploy.staging.path" value="/var/opt/sukrupa/school-admin/"/>
    <property name="web.server.jar" value="web-server.jar"/>
    <property name="web.server.class.name" value="org.sukrupa.platform.server.WebServer"/>

    <property name="tools.dir" value="tools" />
    <property name="tools.ant.dir" value="${tools.dir}/apache-ant-1.8.2" />

    <property name="ops.dir" value="ops" />
    <property name="ops.build.dir" value="${ops.dir}/build" />

    <property name="ops.deploy.dir" value="${ops.dir}/deploy" />
    <property name="ops.deploy.sql.dir" value="${ops.deploy.dir}/sql" />
    <property name="ops.deploy.insert.fake.data.sql" value="${ops.deploy.sql.dir}/insert-fake-data.sql"/>
    <property name="ops.deploy.delete.fake.data.sql" value="${ops.deploy.sql.dir}/delete-fake-data.sql"/>

    <property name="ops.scripts.dir" value="${ops.dir}/scripts" />

    <property name="twist-config" value="${src.test.functional.dir}/twist-conf"/>
    <property name="cobertura.dir" value="tools/cobertura-1.9.4.1" />
</project>
